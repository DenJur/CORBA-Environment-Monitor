# Stores program settings.
class Settings:

    def __init__(self, name, steps, time, centre, load):
        self.name = name
        self.time = time
        self.steps = steps
        self.centre = centre
        self.load = load
